name: awty_engine
description: "Are We There Yet? Engine - Native step tracking service"
publish_to: 'none'

version: 0.0.2+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.awty.engine
        pluginClass: AwtyPlugin 